log:
  level: debug
  disable_sentry: true

db:
  url: db_url

listener:
  addr: :8000

cop:
  disabled: true
  endpoint: "http://..."
  upstream: "http://..."
  service_name: link-shortener-svc
  service_port: 80

short_url:
  base_url: "http://localhost:8000/integrations/link-shortener-svc/"

